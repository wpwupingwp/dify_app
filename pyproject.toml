[project]
name = "dify_app"
version = "0.1.0"
description = "LLM apps for plant"
authors = [
    {name = "wp", email = "wpwupingwp@outlook.com"},
]
dependencies = ['biopython', 'quart', 'uvicorn', 'aiohttp', 'torchmetrics',
'torch']
requires-python = "==3.11.*"
readme = "README.md"
license = {text = "AGPL-3"}

[tool.pdm]
distribution = false
[project.script]
test = 'uvicorn pubmed:app --port 2024'
